
const initialState = {
    problem: ''
};

const a = 'a';

export default function calcReducer(state = initialState, action){
    switch(action.type){
        default:
            return state;
    }
}
